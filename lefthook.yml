pre-commit:
  parallel: false
  commands:
    biome:
      glob: "*.{ts,tsx,json}"
      run: bunx biome check --staged --no-errors-on-unmatched
      stage_fixed: true
    ast-grep:
      glob: "*.{ts,tsx}"
      run: ast-grep scan {staged_files}
    typecheck:
      run: bun run check
