{
	"name": "@ctrl/infra.otel",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"dev": "pkill -f otelcol-contrib 2>/dev/null || true; rm -f /tmp/ctrl-page-telemetry.jsonl && otelcol-contrib --config=otel-collector.yaml",
		"dev:tui": "otel-tui --http 4319 --grpc 4316 --disable-internal-metrics"
	}
}
