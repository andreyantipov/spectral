/** Event name for state-sync snapshot publication */
export const STATE_SYNC_EVENT = "state-sync" as const;

/** Command action dispatched by UI when webview is mounted and ready */
export const UI_READY_ACTION = "ui.ready" as const;

/** Sentinel group ID: auto-select the first available group in layout */
export const AUTO_GROUP = "__auto__" as const;
