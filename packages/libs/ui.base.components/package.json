{
	"name": "@ctrl/ui.base.components",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"exports": {
		".": "./src/index.ts",
		"./styles.css": "./build/styles.css"
	},
	"scripts": {
		"check": "tsgo --noEmit",
		"prepare": "panda codegen",
		"build": "panda codegen && mkdir -p build && panda cssgen --outfile build/panda.css && cat src/design/tokens/design-tokens.css build/panda.css > build/styles.css && rm build/panda.css",
		"storybook": "storybook dev -p 6006",
		"build-storybook": "storybook build"
	}
}
