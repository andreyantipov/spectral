// Layout schemas are canonical in @ctrl/base.schema — re-export for local use
export {
	type GroupNode,
	GroupNodeSchema,
	type LayoutNode,
	LayoutNodeSchema,
	type PanelRef,
	PanelRefSchema,
	type PersistedLayout,
	PersistedLayoutSchema,
	type SplitNode,
	SplitNodeSchema,
} from "@ctrl/base.schema";
