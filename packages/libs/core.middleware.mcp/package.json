{
	"name": "@ctrl/core.middleware.mcp",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"check": "echo 'skipped — tsgo TS2589 with @modelcontextprotocol/sdk zod generics on linux'"
	},
	"dependencies": {
		"@ctrl/core.contract.event-bus": "workspace:*",
		"@ctrl/core.contract.state-sync": "workspace:*"
	}
}
