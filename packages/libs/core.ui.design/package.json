{
	"name": "@ctrl/core.ui.design",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"exports": {
		".": "./src/index.ts",
		"./styles.css": "./build/styles.css"
	},
	"scripts": {
		"prepare": "panda codegen",
		"build": "panda codegen && mkdir -p build && panda cssgen --outfile build/panda.css && cat src/tokens/design-tokens.css build/panda.css > build/styles.css && rm build/panda.css",
		"check": "tsgo --noEmit"
	}
}
