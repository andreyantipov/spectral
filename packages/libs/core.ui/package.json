{
	"name": "@ctrl/core.ui",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"exports": {
		".": "./src/index.ts",
		"./styles.css": "./build/styles.css"
	},
	"scripts": {
		"prepare": "panda codegen",
		"build": "panda codegen && mkdir -p build && panda cssgen --outfile build/panda.css && cat src/tokens/design-tokens.css build/panda.css > build/styles.css && rm build/panda.css && tsgo --build",
		"check": "tsgo --noEmit",
		"storybook": "storybook dev -p 6006",
		"build-storybook": "storybook build"
	},
	"dependencies": {
		"@ctrl/core.ui.api": "workspace:*",
		"@ctrl/core.port.event-bus": "workspace:*",
		"@zag-js/toast": "^1.36.0"
	}
}
