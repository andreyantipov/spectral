{
  "name": "ctrl.page",
  "private": true,
  "workspaces": ["packages/apps/*", "packages/libs/*", "packages/infra/*"],
  "scripts": {
    "dev": "turbo dev",
    "dev:desktop": "turbo dev storybook --filter=@ctrl/desktop --filter=@ctrl/core.ui",
    "build": "turbo build",
    "check": "turbo check",
    "lint": "turbo lint",
    "test": "turbo test"
  },
  "packageManager": "bun@1.3.9",
  "devDependencies": {
    "turbo": "latest",
    "@typescript/native-preview": "latest"
  }
}
