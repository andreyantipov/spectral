# Monorepo structure rules
# GritQL equivalents: no_package_deps.md, no_latest_in_deps.md, single_index_per_package.md
# NOTE: no_package_deps and no_latest_in_deps operate on JSON — ast-grep has limited JSON support.
# These rules may need a custom script instead.
# single_index_per_package requires file path matching without code patterns.

# Placeholder — these rules are better handled by:
# - dependency-cruiser (already covers import boundaries)
# - Custom script checking package.json files
# - Biome/ESLint for JSON validation
# See comparison notes in PR description.

id: placeholder-monorepo-note
language: TypeScript
severity: hint
message: "Monorepo JSON rules (no_package_deps, no_latest_in_deps, single_index_per_package) require JSON language support or custom scripts"
rule:
  pattern: PLACEHOLDER_NEVER_MATCHES_$$$
